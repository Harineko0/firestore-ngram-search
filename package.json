{
  "name": "firestore-search",
  "version": "1.0.0",
  "description": "Search firestore documents by text.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": ["dist"],
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "firebase": "^9.0.1",
    "firebase-admin": "^9.11.1",
    "firebase-functions": "^3.15.5"
  },
  "devDependencies": {
    "ts-loader": "^9.2.5",
    "typescript": "^4.4.2"
  }
}
